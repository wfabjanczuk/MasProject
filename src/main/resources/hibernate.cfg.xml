<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/hello-world</property>
        <property name="connection.username">root</property>
        <property name="connection.password">root</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQL55Dialect</property>

        <property name="hibernate.hbm2ddl.auto">create</property>

        <!-- Activate Hibernate Statistics -->
        <property name="hibernate.generate_statistics">true</property>

        <!-- Use Annotation-based mapping metadata -->
        <mapping class="exampleEntity.Guide" />
        <mapping class="exampleEntity.Student" />
        <mapping class="exampleEntity.Message"/>
        <mapping class="exampleEntity.Person"/>
        <mapping class="exampleEntity.Address"/>
        <mapping class="exampleEntity.Customer"/>
        <mapping class="exampleEntity.Passport"/>
        <mapping class="exampleEntity.Movie" />
        <mapping class="exampleEntity.Actor" />
        <mapping class="exampleEntity.Employee" />
        <mapping class="exampleEntity.Friend" />
        <mapping class="exampleEntity.Item" />

    </session-factory>
</hibernate-configuration>















